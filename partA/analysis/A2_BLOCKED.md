# A2 Blocked: Missing Original Artifact

The assignment requires an audit of the original `fertility.py` and its output. That file is not present in the current workspace.

## Required artifact
- `fertility.py`
- associated report or benchmark results
- any starter directory containing the original benchmark setup

## Why the analysis is blocked
The original script is needed to:
- run the exact baseline experiment
- capture the before output
- isolate the specific bug and metric issue
- verify the size of the distortion
- apply the smallest correction and compare before/after outputs

Without the original source, it would be fabrication to claim a bug, a metric problem, or a corrected number.

## Evidence from the workspace
The workspace contains:
- `FLORES_200_4_Languages.xlsx`
- a newly created A1 corpus analysis script

The following required files were not found:
- `fertility.py`
- `REPORT_v0.md`
- `bench/model_spec.md`
- `bench/bench_log.csv`
- `corpus_sample/`

No git repository exists in the current workspace, so no prior commit or branch could be used to recover the missing artifacts.

## What would be needed to continue legitimately
A verified copy of the original starter kit or the original `fertility.py` plus benchmark logs would be required to perform the A2 evidence-based audit properly.

## Conclusion
A2 is blocked by missing source artifacts and cannot be completed without a legitimate recovered copy.
